---
- name: Publish Web Content using Httpd
  hosts: node1
  become: true

  taska: 
    - name: Installing Httpd package
      yum:
        name: httpd
        state: latest

    - name: Start & enable httpd service
      service:
        name: httpd
        state: started
        enabled: true


