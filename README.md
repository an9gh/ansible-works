# Ansible Control Node Setup

This repository contains the configuration files and automation playbooks used to manage infrastructure nodes.

## Project Structure
- `ansible.cfg` - Central Ansible configuration settings.
- `inventory.ini` - Target managed node device addresses and groups.
- `play.yml` - Core playbook for connectivity testing and automation tasks.

